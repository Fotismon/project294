services:
  - type: web
    name: hellenic-batteries-api
    runtime: python
    rootDir: battery-analyst-console/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    healthCheckPath: /health
